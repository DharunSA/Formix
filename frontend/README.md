# Frontend

Next.js (TypeScript) client for Formix. See the [repository root README](../README.md)
for full setup instructions, architecture overview, database schema, and API docs.

Quick start:

```bash
npm install
cp .env.example .env.local
npm run dev
```
